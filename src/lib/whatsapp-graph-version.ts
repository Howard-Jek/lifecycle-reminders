/**
 * One source of truth for the Meta Graph API version, matching the host app.
 * Bumping Meta's version is a single-line change here and nowhere else.
 */
export const GRAPH_API_VERSION = "v21.0"
